#Angular2 starter project with TypeScript, Chrome Debugging & Bootstrap

This is a simple `angular` project template that is setup with `TypeScript` and debugging. It also uses a simple `bootstrap` ui.